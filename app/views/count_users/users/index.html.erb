<h2>Users</h2>

<table class="table">
  <thead>
    <th>Email</th>
    <th>Joined at</th>

  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.email %></td>
        <td><%= user.created_at %></td>
      </tr>
    <% end %>
    </tbody>
</table>
